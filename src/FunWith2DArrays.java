import java.util.Arrays;

public class FunWith2DArrays
{
    public FunWith2DArrays() {};

    public static int totalElements(int[][] intArray)
    {
        int numRows = intArray.length;
        int numColumns = intArray[0].length;
        return numRows * numColumns;
    }

    public static String fourCorners(String[][] stringArray)
    {
        return "Osmanthus wine tastes the same as I remember, but where are those who share the memory?";
    }
}
